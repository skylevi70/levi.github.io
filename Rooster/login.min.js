// JavaScript Document
 
    var count = 2;
    function validate() {
    var un = document.myform.username.value;
    var pw = document.myform.pword.value;
    var valid = false;
    var unArray = ["Recmo", "Levi", "username3", "username4"];  // hier de namen 
    var pwArray = ["Remo#", "Lemo$", "password3", "password4"];  // hier je wachtwoorden;
    for (var i=0; i <unArray.length; i++) {
    if ((un == unArray[i]) && (pw == pwArray[i])) {
    valid = true;
    break;
    }
    }
    if (valid) {
    document.getElementById('login').style.display = ('none')
    document.getElementById('rooster').style.display = ('inline')
    alert ("Login was sucessfully processed.  You will be redirected to the members page now.");
    return false;
    }
    var t = " tries";
    if (count == 1) {t = " try"}
    if (count >= 1) {
    alert ("Invalid username and/or password.  You have " + count + t + " left.");
    document.myform.username.value = "";
    document.myform.pword.value = "";
    setTimeout("document.myform.username.focus()", 25);
    setTimeout("document.myform.username.select()", 25);
    count --;
    return false;
    }
    }